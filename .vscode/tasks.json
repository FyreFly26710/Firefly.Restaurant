{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "dev: postgres up",
      "type": "shell",
      "command": "docker compose -f src/server/docker-compose.pgsql.yml up -d firefly-restaurant-postgres",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [],
      "presentation": {
        "reveal": "silent",
        "panel": "dedicated"
      }
    }
  ]
}
